# 레인 A — 알고리즘 · 백테스트 (검증용)

**소유**: `model/` · `service/precompute.py` · `docs/model-findings.md`
**산출**: `grid_score` 테이블 + 검증 리포트
**읽기만**: `pipeline/`, `probe/`

---

## 현재 도달점

| | |
|---|---|
| 순수 입지 모델 | **LightGBM (현행 유지)** · AUC **0.6369** (최고 베이스라인 0.5669) · seed=0 = 배포분 |
| 상위 10% 자리 | 실측 3년 생존율 **76.9%** (73.8–79.7) · 전체 58.6% |
| 하위 10% | **28.4%** (25.4–31.6) · 십분위 격차 48.4%p |
| 학습/검증 | 2005–2022 / **2023**, 교집합 0 · 순위 세트 LOC2(20) |
| 누수 가드 | RED(오염 AUC 1.0000 탐지) → GREEN PASS · 신규 피처 ≤T 셀프테스트 PASS |

강건성: 연도별 1.18~1.25x(코로나 포함) · 업태 8종 전부 1.12~1.28x · **자치구 24/25 개선**
(25/25는 면적 포함 NUM 계보의 값이다 — 순수 입지 계보에서는 성북구가 0.98x로 빠진다)
변인통제: 면적 25㎡ 미만 격차 **28.0%p** ↔ 90㎡ 이상 **15.1%p** (`score_meta.grade_area`,
legacy 벤치 train 2005–2018 / test 2019–2022 — 화면이 내보내는 값이 이것이다).
이전에 적혀 있던 37.6 / 14.3 은 출처와 계보가 어디에도 기록돼 있지 않아 지웠다.

## 알아야 할 제약 — 어기면 결과가 조용히 거짓이 된다

1. **as-of 원칙.** 피처는 개업 시점 이전 정보만. 새 피처를 넣을 때 `model/asof.py`의 `FEATURES`에 관측 시점 근거를 반드시 적고, `python -m model.test_leakage`로 RED→GREEN 확인.
2. **면적·층은 순위에서 제외.** 점포 속성이지 입지가 아니다. 면적만으로 AUC 0.6227이 나오므로 섞으면 "자본력 예측기"가 된다. 순위는 `LOC` 계열 피처셋으로.
3. **베이스라인을 약화시키지 않는다.** `prior_surv`(학습 없이 그 자리 과거 생존율)를 못 이기면 모델은 무가치하다.
4. **개선이 없으면 없다고 기록한다.** 임계·split·지표를 바꿔 통과시키지 않는다. 이미 negative result 2건이 문서에 남아 있다.

## 시도했고 안 된 것 — 반복하지 말 것

| 시도 | 결과 |
|---|---|
| 인허가 파생 피처 7개 추가 (수요군 경쟁·영업연수·엔트로피·폐업가속) | **AUC 변화 0.0000.** 중요도 상위에 오르지만 정보 중복 |
| 네이버 검색 트렌드 (행정동 단위) | logit −0.0004 / gbm +0.0014 |
| 검색 트렌드 재검정 (위약 대조 30회 · 모델 3종) | gbm +0.0042 하나만 양성, logit은 음수(CI가 0 배제) · 족내 p≈0.09 — **위양성** |
| 점포별 블로그 언급량 (점 단위, 마포구 17,056개) | logit −0.0035 / gbm −0.0125 |
| Tier1 인허가 재파생 8개 (공실 회전·체인 비중·격자 나이·업태 전환 등) | 내부검증은 전부 양수였으나 번들 홀드아웃 ΔAUC +0.0006, CI∋0 |
| Tier2 휴게음식점 5개 (카페·패스트푸드 밀도·동학, 146,507건 신규 수집) | 〃 (같은 번들에 포함) |
| Tier3 지하철 승하차 (역 단위 월별, 2015-01~) | 부표 ΔAUC +0.0004 [−0.0005, +0.0013] · 커버리지로 편입 불가 |
| E6 동급 상권 군집화 | 실루엣 0.1175 · η² 0.1327 (요구 0.25 / 0.20) — 실패 |
| 모델 교체 (후보 6종, AUC 기준·십분위 기준 2회) | 십분위 기준에서 Δ상위10% −0.12%p [−0.94, +0.98] — 기각, 현행 유지 |

**결론: 인허가 이력 파생은 포화이고, 새 외부 원천 5종이 전부 이걸 못 깼다.** 지하철 접근성의 "+0.0025"는 CI 없는 단일 적합 차이였고, 짝지은 부트스트랩으로는 판별 불가(§7 G4). 실제로 이긴 것은 **학습 기간 확대(E1)와 모델 교체(E-M)** 두 가지다.

## 다음 후보

**실행 설계 확정본: `docs/experiment-plan.md` (2026-07-27, 사전 등록 기준·우선순위·일정 포함). 아래 목록은 그 원천.** 절제 프로그램 골격은 `model/ablation.py`(미구현 스텁 — 채우기 전엔 실행 불가하게 만들어 둠).

- 다른 자치구에서 언급량 재현 (마포구는 지역 내 격차가 작아 검증력이 약했다 — 베이스라인 AUC 0.5452)
- 승계 제외 라벨로 재학습 (현재는 포함. 제외 시 생존율 +1.87%p)
- 학습 기간 2005~2018로 확대 (현재 2013~)
- horizon 1년/5년 모델
- 외부 정보: 버스정류장·대로 접면 (VWORLD 승인되면 물리 입지)

## 명령

```bash
python -m model.asof --selftest --describe   # 시간의존성 · 피처 관측시점
python -m model.test_leakage                 # 누수 가드
python -m model.evaluate --models logit,gbm --assert-beats-baseline
python -m model.backtest                     # 십분위 실측
python -m model.robustness                   # 상식규칙·연도·업태·지역
python -m model.stratified                   # 면적 통제
python -m service.precompute                 # grid_score 재계산 (84초)
python -m model.profile_build                # text_profile 재집계 + 커버리지 실측
```

## 레인 B와의 계약

`grid_score(uptae, grid_id, score, grade, observed)` + `score_meta`. **스키마를 바꾸려면 B에 먼저 알린다.** 모델 내부는 자유롭게 바꿔도 된다.

### `text_profile` — 신규 (2026-07-28, §17)

비정형에서 나온 유일한 제품 산출물. **등급과 무관한 별도 영역이다.**

```
text_profile(grid_id PK, n_post, price_med, price_q1, price_q3, price_n,
             gripe_n, gripe_share, gripe_shops,
             guest_party, guest_purpose, guest_n)
```

- **격자 단위 조회.** 값은 반경 r1(3×3, 약 300m) 집계다
- `gripe_shops` 형식 `"<범주>:<상호1>|<상호2>;<범주>:..."`
- `price_*` 는 **전부 NULL이다.** §I-9가 기각돼(ρ +0.138) 집계하지 않는다. 컬럼은 스키마 호환을 위해 남겨뒀을 뿐이니 **읽지 말 것**
- 행이 없는 격자는 표본 미달(r1 글 10건 미만)이다. **0으로 채우지 말 것**

**`gripe_shops` 는 세 상태가 다르다. 하나로 세지 말 것** — 합쳐 세는 바람에 "약점 목록 5,065격자"라고 인용해 왔는데 실제로 점포가 실린 곳은 3,056이다.

| 값 | 뜻 | 화면 |
|---|---|---|
| `NULL` | r1 판정 글이 10건 미만 — **못 본 것** | `근거 부족` |
| `""` | 충분히 봤는데 불만이 없음 — **본 것** | `관측된 문제 없음` |
| `"wait:A\|B;..."` | 목록 있음 | 목록 |

노출 규칙은 `score_meta`에 선언돼 있다. 코드에 하드코딩하지 말고 이 값을 읽어라.

```
text_profile_exposed             (빈 값)        ← 노출 승인된 항목이 없다
text_profile_exposure_note       I-20:gripe reject(precision 0.087-0.167)
                                 guest:precision untested
text_profile_gripe_judged_grids  5065           ← 판정 충분
text_profile_gripe_listed_grids  3056           ← 실제 목록 보유
text_profile_gripe_gate2_grids   601            ← 한 범주에 독립 점포 2곳 이상
text_profile_guest_grids         557
text_profile_claim               observation_only:not_predictive
text_profile_verdicts            I-11:agree_pass · I-20:precision_reject · I-13:pass · I-9:reject
```

## `text_profile` 은 화면에 내지 않는다 (2026-07-28, §18)

**§I-20 에서 `gripe` 7범주가 전부 정밀도 기각됐다** — 8.7~16.7%, 기준 0.70. 1차가 불만을 붙인 글의 32~61% 에서 두 독립 판정자가 모두 "그런 불만 없다"고 했다.

§I-11 의 일치율 0.996 은 철회하지 않는다. 그것은 **재현성** 검정이고 통과했다. **같은 라벨을 두 번 내놓는 것과 그 라벨이 맞는 것은 다르다** — 300건×7범주=2,100셀에서 positive 기대값이 13.3셀이라 0.996 은 "둘 다 false" 가 만든 값이었다.

- 테이블과 컬럼은 남긴다. 재판정 후 다시 재려면 비교 대상이 필요하다
- **`exposed` 가 비어 있으면 아무것도 내지 않는다.** "아직 안 정했다"가 아니라 "내지 말라"다
- 다시 노출하려면 §I-19 기준(정밀도 ≥0.70 · Wilson 하한 ≥0.50 · kappa ≥0.40)을 통과해야 한다

**지켜야 할 것 둘.**

1. **생존율·등급과 연결짓지 말 것.** 예측력은 아홉 번 기각됐다. "대기 불만이 많으면 생존율이 높다"류의 문장은 근거가 없다
2. **`parking`을 나머지와 한 목록에 두지 말 것.** 창업자가 고칠 수 없는 입지 제약이라, 섞으면 못 고칠 것을 고칠 것처럼 읽힌다
