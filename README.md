# 서울 요식업 입점 위치 추천

공모전 출품작의 기술 파트. 서울 어느 지점이든 **100m 격자** 단위로 경쟁·생존·수요·비용을 조회하고, **과거 데이터로 검증된** 입지 등급을 매긴다.

**상태: 데이터 파이프라인 + 생존예측 모델 + 비용 시나리오 완료.** UI 미착수.

```
verify 8/8 · consistency 17/17 · as-of selftest PASS · 누수 가드 PASS
```

---

## 한 문단 요약

서울 일반음식점 인허가 **53.5만 건**(1924~2026, 개·폐업 전체 이력)을 100m 격자에 올리고, 개업 시점의 상권 상태를 인허가 이력만으로 복원해(as-of) 3년 생존 여부를 학습했다. 학습 2013–2018 / 검증 2019–2022로 시간 분리했고, **상위 10% 등급 자리의 실측 3년 생존율은 74.3%, 전체 평균은 61.7%**다. 여기에 실측 생존곡선을 곱해 "이 자리에서 이 임대료면 3년 뒤 얼마가 남는가"를 계산한다.

---

## 핵심 수치

| | |
|---|---|
| 인허가 이력 | 535,603건 · 좌표 보유 89.1% |
| 100m 격자 | 21,544개 (데이터가 닿는 셀) |
| 상권 내 격자 | 11,466 (53.2%) — 나머지는 매출 NULL |
| 서울 3년 생존율 | 2013년 개업 71.3% → 2019년 65.7% → 2023년 58.8% |
| 모델 (순수 입지) | AUC 0.5909 · 최고 베이스라인 0.5559 |
| 상위 10% 자리 | 실측 생존율 **74.3%** (전체 61.7%) |
| 하위 10% 자리 | 실측 생존율 **44.8%** |

**입지 위험의 가격**: 매출·비용이 완전히 같아도 상위 10% 자리는 3년 기대손익 +34만, 하위 10%는 −1,895만. 차이 1,929만원이 순수하게 생존확률에서 나온다.

**타깃이 분명하다**: 작은 가게일수록 입지가 중요하다. 25㎡ 미만은 상위/하위 격차가 **33.7%p**인데 90㎡ 이상은 10.4%p다. **자본이 적을수록 이 서비스가 더 필요하다.**

---

## 실행

```bash
python -m pipeline.run --all                  # 수집 → 정규화 → 격자 → 검증
python -m model.recommend --uptae 한식 --top 10        # 입지 추천
python -m model.economics --uptae-cd CS100001 --rent 300 --upfront 8000
python -m pipeline.query --lon 127.0276 --lat 37.4979  # 좌표로 격자 조회
```

검증:
```bash
python -m pipeline.verify        # 적재 8종
python -m pipeline.consistency   # 논리 정합성 17종
python -m model.test_leakage     # 누수 가드 RED→GREEN
python -m model.backtest         # 십분위 실측 생존율
python -m model.robustness       # 상식 규칙 대비·연도·업태·지역
python -m model.stratified       # 면적 통제 후 입지 효과
```

`.env`에 키가 필요하다(커밋 금지). 수집분은 `pipeline/cache/`에 캐시되어 재실행 시 API를 다시 부르지 않는다.

---

## 문서

| 파일 | 내용 |
|---|---|
| `docs/data-inventory.md` | 데이터 소스별 실측 결과 · 공간해상도 판정 |
| `docs/model-findings.md` | 모델 결과 · 실효성 검증 · **negative result 2건** |
| `docs/api-applications.md` | 직접 신청해야 하는 API (공정위·R-ONE·VWORLD) |
| `docs/experiment-plan.md` | 제출 전 실험 라운드 2 설계 (사전 등록 기준·일정) |
| `docs/submission.md` | **제출물 설계** — zip 패키징 · 기술설명서 서사 교체 매핑 · 마감 8/3 16:00 |
| `frontend/design/ui-spec.md` | UI 스펙 v2 — 시안 대비 차분 · 플로우 · 컴포넌트 규칙 |
| `docs/tracking/criteria-*.md` | 수용 기준과 통과 증거 |

## 코드

| 모듈 | 역할 |
|---|---|
| `pipeline/config.py` | 좌표계·업종코드·기준선 단일 출처 |
| `pipeline/grid.py` | 100m 격자(EPSG:5179) · 좌표계 변환 |
| `pipeline/collect·normalize·geocode·features` | 수집 → 정규화 → 행정동 → 격자 피처 |
| `pipeline/sgis·access·trend·mentions` | 센서스 · 지하철 접근성 · 검색 트렌드 · 점포 언급량 |
| `pipeline/cohort.py` | 개업 코호트 생존율 |
| `pipeline/verify·consistency` | 적재 검증 / 논리 정합성 |
| `model/asof.py` | **as-of 피처 복원 — 누수 차단의 핵심** |
| `model/dataset·evaluate·train` | 라벨 · 평가 하니스 · 모델 |
| `model/recommend·economics` | 입지 추천 · 손익 시나리오 |
| `model/backtest·robustness·stratified` | 실효성 검증 |
| `probe/` | 실측 근거 (수정 금지) |

---

## 알아야 할 다섯 가지

### 1. 좌표계는 문서가 아니라 실측으로 정했다
인허가는 **EPSG:5174**, 서울 상권은 **EPSG:5181**. 서로 다르다.

후보 좌표계들은 원점을 공유하고 datum만 달라, 틀린 걸 골라도 점이 서울 안에 그럴듯하게 떨어진다. 실제로 처음 EPSG:2097로 가정했고 **내부 검증 14/14가 통과하는 상태에서 모든 점포가 1~2 격자 밀려 있었다.** 역지오코딩으로 각 레코드의 주소와 대조해서야 잡혔다. 좌표계를 바꾸려면 `probe/p8_crs.py`부터 돌릴 것.

### 2. 데이터가 없으면 NULL이지 0이 아니다
상권 밖 격자는 매출·유동인구가 NULL이다. 0을 넣으면 그 자리가 "최악의 입지"로 랭크돼 추천이 조용히 거짓이 된다. `verify nullzero`와 `consistency nullnotzero`가 이걸 지킨다.

### 3. 거친 데이터를 격자로 배분하지 않는다
행정동 인구를 격자에 나눠 담으면 산과 하천에도 인구가 배정된다. 거친 값은 **"이 격자가 속한 단위의 값"**으로 표기해 붙이고, 같은 행정동 격자들은 그 피처에서 동점이 된다. **격자 간 순위는 점 단위 피처만이 결정한다.**

### 4. 생존율의 분모는 관측 가능한 부분이다
`survive_Ny = 1 − (N년 내 폐업) / (개업 후 N년이 이미 지난 점포)`. 2025-11 개업 점포는 3년 테스트를 못 치렀으므로 분자·분모 어디에도 안 들어간다. **"폐업한 가게 중 N년 못 버틴 비율"과 혼동하지 말 것** — 그건 실패에 조건부라 예측에 못 쓴다.

### 5. 점포 속성과 입지를 섞지 않는다
면적은 단일 최강 예측자다(면적만으로 AUC 0.6227, 25㎡ 44.8% ↔ 90㎡+ 74.4%). 하지만 **점포 속성이지 입지가 아니고**, 추천에서는 사용자가 정하므로 모든 후보에 같은 값이 들어간다. 순위·보정 모두 면적을 뺀 모델을 쓴다. 층 정보도 마찬가지(지하 59.8% / 1층 66.2% / 2층 69.9%).

---

## 정직하게 남겨둔 것

- **비정형은 기여하지 않았다.** 검색 트렌드(지역 단위)와 점포 언급량(점 단위) 두 방식 모두 측정했고 둘 다 개선이 없었다. "결합했다"고 주장하지 않고 "측정했고 아니었다"고 쓴다.
- **인허가 이력 파생 피처는 포화**다. 7개를 추가해도 AUC 변화 0.0000. 외부 정보(지하철 접근성)만 소폭 개선했다.
- **상위 10% 자리에서도 약 26%가 3년 내 폐업**하고, 하위 10%에서도 44.8%가 살아남는다. 입지는 일부일 뿐이다.
- **AUC 0.59는 무작위보다 나은 수준**이지 "잘 맞힌다"가 아니다.
- 미확보: 공정위 창업비용(승인 확인 필요) · R-ONE 임대료(키 필요) · VWORLD 물리입지(미승인) · 집계구 경계(오픈API 미제공)

---

## 다음

1. **UI** — 서비스 계층(`service/`)이 JSON을 내주므로 그 위에 붙인다
2. 공정위·R-ONE 키 확보 → 비용 축 확장 (`docs/api-applications.md`)
3. 다른 자치구에서 비정형 재현 (마포구는 지역 내 격차가 작아 검증력이 약했다)
