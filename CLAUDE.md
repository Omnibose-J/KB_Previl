# 서울 요식업 입점 위치 추천 — 프로젝트 지침

공모전 출품작. 서울 어느 지점이든 100m 격자 단위로 입지를 평가하고, **과거 데이터로 검증된** 등급을 매긴다. 데이터 파이프라인·생존예측 모델·비용 시나리오는 완료됐고 UI는 스펙 대기 중이다.

## 시작하기 전에 읽을 것

1. `lanes/README.md` — **3레인 병렬 작업 규칙 (필수)**
2. 자기 레인 문서 — `lanes/A-algorithm.md` / `lanes/B-backend.md` / `lanes/C-frontend.md`
3. `README.md` — 전체 현황과 핵심 수치
4. 필요 시 `docs/model-findings.md`(모델 결과·negative result) · `docs/data-inventory.md`(데이터 실측)

**자기 레인 폴더만 쓴다.** `model/`=A, `service/`=B, `frontend/`=C. `pipeline/`은 공유라 변경 전 협의, `probe/`는 읽기 전용.

## 절대 어기지 말 것

이 규칙들은 전부 **실제로 사고가 났던 지점**이다.

1. **데이터가 없으면 NULL이지 0이 아니다.** 상권 밖 격자(49.5%)에 매출 0을 넣으면 "최악의 입지"로 랭크돼 추천이 조용히 거짓이 된다.
2. **좌표계는 실측으로 정한다.** 인허가=EPSG:5174, 상권=EPSG:5181. 처음 2097로 가정했을 때 **내부 검증 14/14가 통과하는 상태로 전 점포가 1~2격자 밀려 있었다.** 바꾸려면 `probe/p8_crs.py`부터.
3. **거친 데이터를 격자로 배분하지 않는다.** 행정동 값은 "그 단위의 값"으로 표기해 붙이고, 같은 동 격자는 동점이 된다. 순위는 점 단위 피처만 결정한다.
4. **생존율 분모는 관측 가능한 부분.** `1 − (N년 내 폐업) / (개업 후 N년 경과한 점포)`. "폐업한 가게 중 N년 못 버틴 비율"과 다르다.
5. **점포 속성과 입지를 섞지 않는다.** 면적만으로 AUC 0.6227이 나오므로 순위에 넣으면 자본력 예측기가 된다. 층도 같다.
6. **검증을 우회하지 않는다.** 개선이 없으면 없다고 기록한다. 임계·split·지표를 바꿔 통과시키지 않는다.

## 항상 통과 상태여야 하는 게이트

```bash
python -m pipeline.verify        # 적재 8/8
python -m pipeline.consistency   # 논리 정합성 17/17 (역지오코딩 2종은 KAKAO 키 필요)
python -m model.test_leakage     # 누수 가드 RED→GREEN
python -m model.asof --selftest-cut   # 신규 피처: T 이후 행을 지워도 값 불변
```

## 환경

- `.env`에 API 키(커밋 금지). 읽을 때는 `pipeline.config.load_env()` — 직접 파싱 금지
- `kb.db`(229MB)·`pipeline/cache/`(807MB)는 gitignore. 경로는 `KB_DB`/`KB_CACHE`/`KB_ENV`로 덮어쓸 수 있다(worktree 대응)
- `kb.db` **쓰기는 레인 A만**. B·C는 읽기만

## 현재 수치 (인용 시 이 값을 쓸 것)

| | |
|---|---|
| 인허가 이력 | 535,603건 · 격자 **23,572개** (+ 휴게음식점 146,507건) |
| 순수 입지 모델 | LightGBM · AUC **0.6369** (최고 베이스라인 0.5669) — **seed=0 단일 적합 = 배포되는 그 적합.** 시드평균은 0.6392이고 §4-C가 그 값을 seed=0으로 잘못 라벨링했다 |
| 학습/검증 | 2005–2022 / **2023** · 순위 세트 LOC2(20컬럼) |
| 상위 10% 자리 | 실측 3년 생존율 **76.9%** (73.8–79.7) · 전체 58.6% · 하위 10% **28.4%** (25.4–31.6) · 십분위 격차 48.4%p |
| 사전계산 | `grid_score` **241,776행** (12업태 × 20,148격자) — `score_meta`가 헤드라인과 동일 |

**함께 말해야 할 한계**: 상위 10% 자리에서도 약 25%는 3년 내 폐업한다. AUC 0.64는 "무작위보다 나은 수준"이지 "잘 맞힌다"가 아니다. 비정형으로 **예측하려는 시도는 아홉 번 전부 기각됐다** — 예측 정확도 3회(§6·§6-B·§8-G), 지명 단위 3회(§11), 실거래가 1회(§12), 방문 시간대 1회(§16), 1인당 지출 1회(§17). **이유도 쟀다: 검색 관심도와 실제 유동인구의 상관이 ρ=+0.079다**(§11-E). 인허가 재파생·휴게음식점·지하철 승하차·지하철 접근성도 홀드아웃에서 기여를 판별할 수 없었다.

**비정형에서 제품에 남은 것은 없다**(§18·§19). 경쟁자 약점 목록(`text_profile`)이 유일한 채택이었으나 **정밀도 검정에서 기각됐다** — 7범주 전부 8.7~16.7%(기준 0.70). 2차 일치율 0.996은 희소 클래스라 "둘 다 false"가 만든 값이었다. `text_profile_exposed`는 비어 있고, **이 표를 화면에 내지 말 것.** 재현성 통과(§I-11)와 정확도 통과는 다른 것이다. **구제 시도 셋도 전부 기각됐다**(§19): 확장 검색어 0/6, 추출기를 haiku·sonnet으로 교체 0.360/0.615, 가장 잘 검정됐던 `guest.purpose`도 0.533~0.600. 마지막 것은 κ 0.48~0.76으로 **측정이 성공한 상태의 기각**이라 재시도 여지가 없다 — `guest_*` 격자 집계도 제품 승인 0이고, `party`는 재지 않는다. 추출 도구 자체는 인허가 업태를 정답으로 놓은 채점에서 macro-F1 **0.4701**(무작위 0.143)로 통과했다. 가격대(§I-9)는 ρ=+0.138로 기각이라 `text_profile.price_*`는 전부 NULL이다.

**수치 인용 규칙**: 헤드라인은 순수 입지(gbm·LOC2·train 2005–2022 / test 2023) 홀드아웃 십분위 — 76.9% / 28.4% / 폐업 약 23%. **격자수 계열은 2026-07-29 콜드 재구축분을 쓴다** — `store`(SEMAS) 적재 후 `build_grid`를 재실행하지 않아 `licence ∪ store` 합집합 23,572 중 21,544만 담겨 있었다. 추가 2,028 격자는 인허가 이력이 없어 학습·평가 코호트에 들어가지 않으므로 **생존율·AUC·십분위 격차는 불변**이고 점수 대상만 19,113 → 20,148로 늘었다. 21,544 / 229,356 / 19,113 / 46.8%를 인용한 문서를 발견하면 갱신 누락이다. 생존율 계열은 **2026-07-28 `asof.py` 결측 규약 수정 후 재학습분**이다(이력 없는 격자에 `churn_36m=0.0`·`growth_36m=1.0`을 넣던 것을 `None`으로 — 자세한 것은 `docs/model-findings.md` §20). 그 이전 값(75.5% / 29.2% / 46.3%p)을 인용한 문서를 발견하면 갱신 누락이다. **5년 곡선(상위 61.9% / 하위 33.7%)만은 legacy 벤치(train 2005–2018)의 값이므로 1·3년 곡선에 이어 붙이지 말 것** — 코호트가 다르다. 면적 포함 수치는 성능 상한 참고용이며 추천 성능으로 인용 금지. **"자치구 25/25 개선"은 면적 포함(NUM) 계보의 값이므로 순수 입지 서사에 쓰지 말 것** — 순수 입지에서는 24/25다. 계보 전체는 `docs/model-findings.md` §4-C.
